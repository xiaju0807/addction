const express=require('express')
const path=requer('path')
const app=express();

app.use(express.static(path.join(_dirname,'/public');//静态资源文件
                       
app.listen(3000,()=>{
// console.log('localhost:3000')
})                       
